<h1>Active Patient Accounts</h1>

<% @patients_act.each do |pt| %>
	</br><%= pt.phone_number %>, Patient ID: <%= pt.id %>, Physician: <%= Physician.find(pt.physician_id).email %> <%= link_to "Deactivate and Archive", deactivate_and_archive_patient_path(:patient_id => pt.id) %></br>
	<% end %>