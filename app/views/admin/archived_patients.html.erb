<h1>Archived Patient Accounts</h1>

<% @patients_arch.each do |pt| %>
	</br><%= pt.phone_number %>, Patient ID: <%= pt.id %>, , Physician: <%= Physician.find(pt.physician_id).email %> <%= link_to "Reactivate and Unarchive", reactivate_and_unarchive_patient_path(:patient_id => pt.id) %></br>
	<% end %>
